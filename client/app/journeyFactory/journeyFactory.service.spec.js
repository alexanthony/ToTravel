'use strict';

describe('Service: journeyFactory', function () {

  // load the service's module
  beforeEach(module('toTravelApp'));

  // instantiate service
  var journeyFactory;
  beforeEach(inject(function (_journeyFactory_) {
    journeyFactory = _journeyFactory_;
  }));

  // it('should do something', function () {
  //   expect(!!journeyFactory).toBe(true);
  // });

});
